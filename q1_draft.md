# 4 问题一：主流大语言模型综合性能评价

## 4.1 问题分析与建模思路

大语言模型的综合性能具有明显的多维特征，不同模型在复杂推理、知识与事实可靠性、长上下文处理、代码与软件工程以及多模态任务上的表现存在较大差异。因此，仅依赖单一 Benchmark 的测试结果难以全面反映模型的真实能力。

与此同时，本题数据还具有三个显著特点。

第一，不同 Benchmark 的评分尺度、测试难度和评价协议并不统一，原始得分不具有直接可加性。若简单对各测试成绩进行标准化后加权平均，仍可能受到测试尺度和任务难度差异的影响。

第二，不同 Benchmark 之间可能存在较强的信息重叠。同类 Benchmark 若被重复计入综合评价，将导致某一类能力被隐性重复赋权。

第三，各模型公开评测结果并不完整，部分指标还存在由模型功能差异造成的结构性缺失。例如，多模态指标仅适用于具备相应能力且存在公开测试结果的模型。若直接采用零值填补，则会将“未测评”错误解释为“性能为零”。

基于以上特点，本文不直接对原始 Benchmark 分数进行加权，而将不同模型在**同一评测设置下的相对优劣关系**作为基本信息，构建基于 Bradley–Terry 成对比较的大语言模型综合性能评价模型。

具体而言，首先利用缺失感知的 Spearman 秩相关系数分析 Benchmark 之间的相关性和信息冗余，并结合数据覆盖率与模型比较网络的连通性完成指标筛选；其次，在同一 Exact Setting 内构造模型间成对比较关系，并引入性能差距权重以及 Benchmark Family 平衡机制，建立正则化 Bradley–Terry 模型，得到五个能力维度上的潜在能力得分；随后，从信息量、非冗余性和估计稳定性三个方面确定五个维度的客观权重，并计算模型综合得分；最后利用 Bootstrap、Leave-One-Family-Out 和多种敏感性情景检验评价结果的稳健性，并进一步分析 Kimi K3 的竞争优势与现存短板。

因此，问题一的总体建模流程为

\[
\boxed{
\text{公开评测数据}
\rightarrow
\text{缺失感知相关分析}
\rightarrow
\text{指标筛选}
\rightarrow
\text{成对比较}
\rightarrow
\text{正则化BT模型}
\rightarrow
\text{五维能力得分}
\rightarrow
\text{客观赋权}
\rightarrow
\text{综合评价}
\rightarrow
\text{稳健性检验}
}
\]

【图4-1 问题一综合评价模型流程图】

---

## 4.2 评价指标体系构建

### 4.2.1 数据集构成

本文选取当前具有代表性的 10 个主流大语言模型作为评价对象，包括 Kimi K3 (max reasoning)、GPT-5.6 Sol (max)、GPT-5.5 (xhigh)、Claude Fable 5 (max, with fallback)、Claude Opus 4.8 (max)、Gemini-3.1-Pro (High)、DeepSeek-V4-Pro Max、DeepSeek-V4-Flash Max、Qwen3.8-Max 和 GLM-5.2 (max)。

经过模型版本匹配、公开结果核验和人工复核后，共整理得到 14 个 Benchmark Families 和 21 个 Exact Settings。理论上形成 \(10\times21=210\) 个“模型—测试设置”组合，其中获得有效观测 164 项，整体数据覆盖率为

\[
\eta=\frac{164}{210}\times100\%=78.10\%.
\]

全部 164 项核心记录均完成来源追溯及人工核验。为避免后续分析过程中产生数据漂移，本文将正式数据冻结为 \(v1.0\) 版本，后续全部模型计算均建立在该数据集之上。

由于公开测评数据并非完整矩阵，本文将缺失情况分为一般性缺失和结构性缺失。对于没有公开成绩的指标，直接保留为空值；对于不适用于某模型的测试，同样不进行人为赋值。特别地，多模态维度仅有 7 个模型存在有效适用数据，因此在综合评价时不将其余模型的多模态能力记为零分。

【表4-1 模型及数据覆盖情况】

【图4-2 模型—Exact Setting 数据覆盖热力图】

### 4.2.2 分层评价指标体系

考虑到同一能力可能对应多个 Benchmark，而同一 Benchmark 又可能具有不同测试设置，本文构建

\[
\text{综合性能}
\rightarrow
\text{能力维度}
\rightarrow
\text{Benchmark Family}
\rightarrow
\text{Exact Setting}
\]

的四层评价结构。

其中，Exact Setting 是能够直接进行模型间性能比较的最小单元；Benchmark Family 用于归并评价目标相同或高度接近的测试设置；进一步将全部 Benchmark Families 归纳为五个一级能力维度：

\[
C_1:\text{复杂推理},
\]

\[
C_2:\text{知识与事实可靠性},
\]

\[
C_3:\text{长上下文},
\]

\[
C_4:\text{代码与软件工程},
\]

\[
C_5:\text{多模态}.
\]

该结构能够在保留不同公开评测信息的同时，降低同源指标因包含多个测试设置而被重复计权的可能。

【表4-2 五维能力—Benchmark Family—Exact Setting 指标体系】

---

## 4.3 基于缺失感知 Spearman 系数的指标筛选

由于不同 Benchmark 的有效模型集合并不完全一致，若先对缺失值进行插补再计算相关系数，会人为引入不存在的数据关系。因此，本文采用基于共同有效样本的 Spearman 秩相关系数。

设 Benchmark \(j\) 与 Benchmark \(k\) 的共同有效模型集合为

\[
I_{jk}=\{i\mid x_{ij},x_{ik}\text{均存在}\},
\]

其共同样本数为

\[
n_{jk}=|I_{jk}|.
\]

则定义二者的缺失感知 Spearman 相关系数为

\[
\rho_{jk}
=
\operatorname{Spearman}
\left(
X_j^{(I_{jk})},
X_k^{(I_{jk})}
\right).
\]

与完整数据条件下的相关性分析不同，本文在报告 \(\rho_{jk}\) 的同时保留对应的共同样本数 \(n_{jk}\)。这是由于在缺失数据条件下，相关系数的可靠程度不仅取决于系数大小，还取决于实际参与计算的模型数量。

【图4-3 Benchmark Family Spearman 相关系数热力图】

【图4-4 Benchmark Family 共同样本数热力图】

对于高度相关的 Benchmark，本文不采用固定阈值进行机械删除，而综合考虑以下因素：

1. 指标之间的相关程度；
2. 两个 Benchmark 所反映的能力含义是否重复；
3. 指标的数据覆盖率；
4. 删除后模型成对比较网络是否仍保持连通；
5. 该指标是否承担连接不同模型群体的关键作用。

本文以 \(|\rho|\geq0.85\) 作为识别高相关候选指标的参考标准，并分别采用 0.80 和 0.90 作为敏感性阈值进行复核。

最终保留 14 个 Benchmark Families 构成综合评价体系。

【表4-3 Benchmark Family 相关性与筛选结果】

---

## 4.4 基于成对比较的五维能力评价模型

### 4.4.1 模型成对比较关系

不同 Benchmark 的评分范围和测试协议存在差异，因此本文不直接比较不同 Benchmark 的原始分数，而仅在**同一 Exact Setting 内部**比较模型表现。

设模型 \(i\) 与模型 \(j\) 在测试设置 \(s\) 上的成绩分别为 \(x_{is}\) 和 \(x_{js}\)，则定义

\[
y_{ijs}=
\begin{cases}
1, & x_{is}>x_{js},\\
0.5, & x_{is}=x_{js},\\
0, & x_{is}<x_{js}.
\end{cases}
\]

当任一模型在该设置上不存在有效成绩时，不构造成对比较关系。

由此，原本不可直接统一量纲的多 Benchmark 评价问题被转化为统一的模型胜负关系问题。

### 4.4.2 性能差距加权

仅使用胜负关系会将微弱领先和显著领先视为等价。为保留性能差距信息，本文定义模型 \(i\) 与模型 \(j\) 在设置 \(s\) 上的归一化性能差距为

\[
m_{ijs}
=
\frac{|x_{is}-x_{js}|}
{\max_l x_{ls}-\min_l x_{ls}+\varepsilon},
\]

其中 \(\varepsilon\) 为防止分母为零而设置的极小正数。

进一步定义 Margin 权重

\[
w^{(m)}_{ijs}
=
\varepsilon_m+
(1-\varepsilon_m)m_{ijs},
\]

从而使差距较大的模型比较向能力参数估计提供更强证据，而接近持平的比较仍保留基本权重。

### 4.4.3 Benchmark Family 平衡

若直接将所有 Exact Settings 生成的成对比较放入模型，一个包含较多测试设置的 Benchmark Family 将因比较次数更多而获得更高的隐含权重。

为避免这一问题，本文在每个能力维度内部进行 Benchmark Family 平衡，使不同 Family 的总比较贡献保持一致。最终单个成对比较的权重可写为

\[
w_{ijs}
=
w^{(m)}_{ijs}
w^{(f)}_{s},
\]

其中 \(w^{(f)}_s\) 表示经 Exact Setting 和 Benchmark Family 两级归一化后的平衡权重。

这一处理保证了评价结果主要受到模型实际性能差异影响，而不会因某一评测体系设置项目较多而获得不合理优势。

---

## 4.5 正则化 Bradley–Terry 模型

### 4.5.1 潜在能力估计

在第 \(d\) 个能力维度上，设模型 \(i\) 的潜在能力参数为 \(\theta_{id}\)。Bradley–Terry 模型认为模型 \(i\) 优于模型 \(j\) 的概率为

\[
P(i\succ j)
=
\frac{\exp(\theta_{id})}
{\exp(\theta_{id})+\exp(\theta_{jd})}
=
\sigma(\theta_{id}-\theta_{jd}).
\]

根据所有有效成对比较，建立加权对数似然函数

\[
\ell_d(\boldsymbol{\theta})
=
\sum_r
w_r
\left[
y_r\ln p_r
+
(1-y_r)\ln(1-p_r)
\right].
\]

其中 \(r\) 表示一个有效成对比较。

部分维度中存在模型持续胜过其他模型的现象，可能引起完全分离或近完全分离，从而使普通极大似然估计出现参数发散。因此本文在目标函数中加入 \(L_2\) 正则项：

\[
\hat{\boldsymbol{\theta}}_d
=
\arg\min_{\boldsymbol{\theta}}
\left\{
-\ell_d(\boldsymbol{\theta})
+
\frac{\lambda}{2}
\sum_i\theta_{id}^{2}
\right\}.
\]

同时施加

\[
\sum_i\theta_{id}=0
\]

以保证模型的可识别性。

### 4.5.2 五维得分标准化

为增强潜在能力参数的直观性，将每个维度内的 Bradley–Terry 参数转化至 \(0\sim100\) 区间：

\[
S_{id}
=
100
\frac{
\hat{\theta}_{id}-\min_i\hat{\theta}_{id}
}{
\max_i\hat{\theta}_{id}-\min_i\hat{\theta}_{id}
}.
\]

最终得到各模型在五个一级能力维度上的相对得分。

需要指出的是，上述标准化仅发生在同一能力维度内部，其目的在于提高结果可解释性，并没有将不同 Benchmark 的原始分数直接进行跨尺度比较。

【表4-4 各模型五维能力得分及排名】

【图4-5 各模型五维能力评价结果】

---

## 4.6 比较网络的结构诊断

Bradley–Terry 模型的有效估计依赖于模型比较网络具有足够的连通性。因此，本文进一步将每个模型视为节点，只要两个模型在某一有效 Exact Setting 上形成直接比较，即建立连接，并分别考察五个能力维度的网络结构。

结果表明，五个维度的数据结构并不完全相同。

C1 的比较网络整体较为稳定；C2 存在一定程度的 separation，但经正则化处理后模型能够稳定收敛；C3 在完整数据条件下保持连通，但对 AA-LCR 存在较明显的结构依赖，删除该 Benchmark Family 后网络连通性显著下降；C4 的模型比较网络则对 LiveBench Coding 具有一定桥接依赖；C5 由于仅有 7 个模型存在有效适用数据，其能力参数仅在相应模型集合内进行估计。

因此，本文在后续分析中并不将维度得分视为完全无误差的确定值，而进一步利用 Bootstrap 和 Leave-One-Family-Out 方法评价其稳定程度。

【表4-5 五维能力比较网络诊断】

---

## 4.7 基于信息—冗余—稳定性的客观赋权

Bradley–Terry 模型得到的是不同模型在五个能力维度上的相对表现，而五个维度在当前公开评测数据中所包含的有效评价信息并不相同。因此，本文不人为规定各能力的重要程度，而从**区分信息、独立信息和估计稳定性**三个方面构造数据驱动的客观权重。

这里的权重表示的是各维度在**当前评价数据集中的有效信息贡献程度**，而非认为某项能力在实际应用中具有更高的先验重要性。

### 4.7.1 信息量

令第 \(d\) 个能力维度中的标准化得分为 \(S_{id}\)，定义

\[
p_{id}
=
\frac{S_{id}+\varepsilon}
{\sum_i(S_{id}+\varepsilon)}.
\]

其归一化信息熵为

\[
H_d
=
-\frac{1}{\ln n_d}
\sum_i p_{id}\ln p_{id}.
\]

于是定义

\[
I_d=1-H_d.
\]

若某一能力维度能够明显区分不同模型，其得分分布差异较大，对应 \(I_d\) 较高。

### 4.7.2 非冗余度

在五维能力得分之间重新计算缺失感知 Spearman 系数，并定义

\[
R_d
=
1-
\frac{1}{D_d}
\sum_{k\neq d}
|\rho_{dk}|,
\]

其中 \(D_d\) 为与维度 \(d\) 存在有效共同样本的其他维度数量。

\(R_d\) 越大，表明该维度与其他维度之间的信息重复程度越低。

### 4.7.3 稳定度

通过 Bootstrap 重采样得到模型在各维度得分的波动程度。设模型 \(i\) 在第 \(d\) 个维度上的 Bootstrap 标准差为 \(\sigma_{id}^{(B)}\)，定义

\[
U_d
=
\operatorname{median}_i
\left(
\sigma_{id}^{(B)}
\right).
\]

经归一化后定义稳定性指标

\[
T_d
=
\frac{1}{1+\widetilde U_d}.
\]

最终，第 \(d\) 个能力维度的综合信息贡献为

\[
q_d=I_dR_dT_d,
\]

归一化得到客观权重

\[
w_d
=
\frac{q_d}{\sum_{k=1}^{5}q_k}.
\]

计算得到

\[
(w_1,w_2,w_3,w_4,w_5)
=
(0.0134,\ 0.3399,\ 0.1997,\ 0.0600,\ 0.3870).
\]

即 C1—C5 的权重分别为 1.34%、33.99%、19.97%、6.00% 和 38.70%。

其中，C5 和 C2 在当前数据条件下具有较高的信息贡献，而 C1 的客观权重较低。需要强调的是，这一结果意味着 **C1 在当前10个模型和现有公开评测数据上提供的新增区分信息相对有限**，而不能解释为“复杂推理能力本身不重要”。

【表4-6 五维能力客观权重及其构成】

---

## 4.8 综合得分与模型排名

对于模型 \(i\)，设其具有有效评价结果的能力维度集合为 \(D_i\)。考虑到不同模型的数据适用范围并不完全相同，本文采用有效权重重新归一化的方法计算综合得分：

\[
G_i
=
\frac{
\sum_{d\in D_i}
w_dS_{id}
}{
\sum_{d\in D_i}w_d
}.
\]

与此同时，定义

\[
C_i
=
\sum_{d\in D_i}w_d
\]

为模型 \(i\) 的有效权重覆盖率，用于表征其综合得分实际使用的维度信息完整程度。

该处理避免了将未公开或不适用的能力维度直接赋值为零，同时通过有效权重覆盖率将数据完整性显式呈现。

模型综合评价结果见表4-7。

【表4-7 十款大语言模型综合得分与排名】

综合得分排名为：

1. GPT-5.6 Sol (max)，75.183；
2. Claude Fable 5 (max, with fallback)，74.599；
3. Kimi K3 (max reasoning)，49.056；
4. DeepSeek-V4-Pro Max，42.509；
5. Gemini-3.1-Pro (High)，41.433；
6. Qwen3.8-Max，38.610；
7. GPT-5.5 (xhigh)，30.129；
8. Claude Opus 4.8 (max)，27.698；
9. DeepSeek-V4-Flash Max，25.374；
10. GLM-5.2 (max)，25.071。

从综合得分来看，GPT-5.6 Sol 与 Claude Fable 5 位于第一梯队，两者得分非常接近；Kimi K3 以 49.056 分位居第三，与其后的 DeepSeek-V4-Pro Max、Gemini-3.1-Pro 等模型拉开了一定差距。

但由于部分能力维度存在数据适用范围差异，仅依据一次综合评分仍不足以判断名次是否稳定，因此有必要进一步分析排序结果的不确定性。

【图4-6 综合得分及 Bootstrap 置信区间】

---

## 4.9 稳健性与敏感性分析

### 4.9.1 Bootstrap 稳定性检验

为考察有限 Benchmark 样本对结果的影响，本文采用保持 Benchmark Family 层次结构的 Bootstrap 重采样方法，对整个评价流程重复计算 2000 次。

每次重采样均重新完成成对比较构建、Bradley–Terry 能力估计、五维客观赋权以及综合评分，由此得到各模型综合得分和排名的经验分布。

对于模型 \(i\)，定义其进入前三名的概率为

\[
P_i^{\mathrm{Top3}}
=
\frac{1}{B}
\sum_{b=1}^{B}
I(r_i^{(b)}\leq3).
\]

结果显示，Kimi K3 的

\[
P_{\mathrm{Kimi}}^{\mathrm{Top3}}=1.000,
\]

即在本次 2000 次 Bootstrap 重采样中，Kimi K3 均保持在综合排名前三，说明其处于第一梯队之后、第二梯队之前的综合竞争位置较为稳定。

【表4-8 Bootstrap 得分区间、排名区间及 Top-3 概率】

【图4-7 各模型 Bootstrap 排名稳定性】

### 4.9.2 Leave-One-Family-Out 分析

进一步依次删除 14 个 Benchmark Families 中的一个，并重新运行完整评价模型。定义

\[
\Delta r_{if}
=
r_{i,-f}-r_i,
\]

其中 \(r_i\) 为完整模型下的排名，\(r_{i,-f}\) 为删除 Benchmark Family \(f\) 后的排名。

结果表明，大多数单一 Benchmark 的删除不会彻底改变总体竞争格局，但 AA-LCR 与 LiveBench Coding 具有较明显的结构性作用。

其中，C3 的比较网络对 AA-LCR 较为敏感；C4 则存在对 LiveBench Coding 的桥接依赖。这表明当前公开数据在长上下文和代码能力维度仍存在一定的测评来源集中现象，对于这些维度中较小的模型得分差异不宜作过度解释。

【图4-8 Leave-One-Family-Out 排名变化】

### 4.9.3 模型设定敏感性

除删除单个 Benchmark Family 外，本文进一步改变正则化参数、Margin weighting 方法、指标冗余阈值，并分别考察删除 C5、多模态完整样本子集以及五维等权等情景。

总体来看，删除 C5、删除 AA-LCR 以及取消 Margin weighting 对最终排名的影响相对较大。其中，在删除 AA-LCR 或不使用性能差距权重的情景下，Kimi K3 的综合名次均下降 1 位。

这说明 Kimi K3 综合第三名的结论总体较为稳定，但其与相邻模型之间的相对位置仍会受到部分评价结构的影响。因此，本文重点关注“模型处于何种竞争梯队”和“主要优势能力是什么”，而不对较小的名次变化作过度解释。

【表4-9 不同敏感性情景下模型排名】

【图4-9 敏感性分析结果】

---

## 4.10 Kimi K3 的竞争优势与短板分析

为进一步刻画 Kimi K3 相对于其他主流模型的能力结构，定义其在维度 \(d\) 上相对于全部有效模型中位数的优势量为

\[
A_d
=
S_{\mathrm{Kimi},d}
-
\operatorname{median}_{i}(S_{id}).
\]

若 \(A_d>0\)，表示 Kimi K3 在该维度高于模型总体中等水平；反之则表示该维度存在相对短板。

【图4-10 Kimi K3 五维能力雷达图】

【图4-11 Kimi K3 相对于模型中位数的能力差值】

结果显示，Kimi K3 最突出的相对优势位于 **C4 代码与软件工程能力**。其该维度得分较 10 个模型的有效中位水平高出 36.415 分，说明 Kimi K3 在代码生成、软件工程及相关任务上的竞争力较为突出。

相比之下，Kimi K3 当前最明显的相对短板为 **C5 多模态能力**，其得分较有效模型中位数低 20.513 分，表明与领先模型相比仍存在一定差距。

综合而言，Kimi K3 的性能结构呈现出较鲜明的特点：代码与软件工程能力表现突出，整体综合竞争力居于样本模型前列；而在多模态等能力方面仍有较明显的提升空间。

从最终综合评价结果看，Kimi K3 以 49.056 分排名第 3，且在 2000 次 Bootstrap 重采样中进入前三的概率达到 1.000，表明其综合竞争位置具有较强稳定性。

不过，敏感性分析表明删除 AA-LCR 或取消性能差距加权后，其名次均可能下降 1 位，因此 Kimi K3 与邻近模型之间的细微名次差异并非完全不受指标结构影响。

基于上述结果，可以认为 Kimi K3 当前的核心竞争优势主要体现在较强的代码与软件工程能力以及较稳定的综合竞争力，而进一步增强多模态能力、提高不同能力维度间的均衡程度，是其缩小与头部模型差距的重要方向。

---

## 4.11 本问小结

针对主流大语言模型公开评测指标尺度不统一、同类指标存在信息冗余以及数据不完全等特点，本文建立了一套基于成对比较的大语言模型综合性能评价方法。

首先，利用缺失感知 Spearman 相关系数分析不同 Benchmark Families 之间的信息重叠关系，并结合数据覆盖程度和模型比较网络连通性完成指标筛选；其次，仅在相同 Exact Setting 内构造模型胜负关系，并进一步考虑性能差距和 Benchmark Family 平衡，建立正则化 Bradley–Terry 模型，获得模型在复杂推理、知识与事实可靠性、长上下文、代码与软件工程及多模态五个能力维度上的相对能力得分；随后，从信息量、非冗余度与 Bootstrap 稳定性三个方面确定能力维度的客观信息权重，并通过缺失感知的权重重新归一化得到模型综合得分。

结果表明，GPT-5.6 Sol、Claude Fable 5 和 Kimi K3 分别位居综合评价前三位，其中 Kimi K3 综合得分为 49.056，在 2000 次 Bootstrap 中进入前三名的概率为 1.000。能力分解进一步表明，其主要优势集中于代码与软件工程维度，而多模态能力则是当前相对明显的短板。

此外，Leave-One-Family-Out 及多种敏感性检验表明，整体竞争格局具有较好的稳定性，但 C3 对 AA-LCR、C4 对 LiveBench Coding 存在一定结构依赖，且 C5 的有效模型覆盖范围相对有限。因此，对于局部维度中的较小分数差异，应结合公开测评数据的完整性谨慎解释。