# Q2 final outputs — protected

This directory must remain free of ranking results until Q1 metadata declares `q1_finalized=true` with a freeze version, freeze date, and objective weights. The Q2 adapter and writer enforce this guard.
